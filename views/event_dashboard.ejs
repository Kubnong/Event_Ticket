<!DOCTYPE html>
<html lang="th">
  <%- include('partials/head') %>
  <body class="min-h-screen flex flex-col bg-gradient-to-b from-black via-neutral-900 to-zinc-950 text-white font-[Prompt]">
    <%- include('partials/navbar') %>

    <main class="flex-1 mx-auto max-w-6xl px-4 py-14">
      <div class="mb-10">
        <a href="/organizer/dashboard" class="text-red-500 hover:text-red-400 transition-colors mb-4 inline-block">&larr; กลับหน้ารวมกิจกรรม</a>
        <h1 class="text-3xl font-bold text-white">Dashboard: <span class="text-red-500"><%= event.title %></span></h1>
        <p class="text-gray-400 mt-1">รายชื่อและข้อมูลสรุปผู้ซื้อบัตร</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="bg-neutral-900 border border-zinc-800 p-5 rounded-xl">
              <h3 class="text-sm font-medium text-gray-400">ขายไปแล้ว</h3>
              <p class="text-3xl font-bold text-red-500 mt-1"><%= tickets.length %></p>
          </div>
          <div class="bg-neutral-900 border border-zinc-800 p-5 rounded-xl">
              <h3 class="text-sm font-medium text-gray-400">คงเหลือ</h3>
              <p class="text-3xl font-bold text-white mt-1"><%= event.available_tickets %></p>
          </div>
           <div class="bg-neutral-900 border border-zinc-800 p-5 rounded-xl">
              <h3 class="text-sm font-medium text-gray-400">บัตรทั้งหมด</h3>
              <p class="text-3xl font-bold text-white mt-1"><%= event.available_tickets + tickets.length %></p>
          </div>
          <div class="bg-neutral-900 border border-zinc-800 p-5 rounded-xl">
              <h3 class="text-sm font-medium text-gray-400">รายรับ (ประมาณ)</h3>
              <p class="text-3xl font-bold text-white mt-1"><%= (tickets.length * event.price).toLocaleString() %> ฿</p>
          </div>
      </div>

      <div class="mt-10 bg-neutral-900/80 border border-zinc-800 rounded-2xl overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full text-left">
            <thead class="bg-neutral-800/50">
              <tr>
                <th class="px-6 py-4 font-semibold text-gray-300 uppercase tracking-wider">อีเมลผู้ซื้อ</th>
                <th class="px-6 py-4 font-semibold text-gray-300 uppercase tracking-wider">รหัสตั๋ว</th>
                <th class="px-6 py-4 font-semibold text-gray-300 uppercase tracking-wider">วันที่ซื้อ</th>
                <th class="px-6 py-4 font-semibold text-gray-300 uppercase tracking-wider">สถานะ</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-zinc-800">
              <% if (tickets && tickets.length) { %>
                <% tickets.forEach(ticket => { %>
                  <tr class="hover:bg-neutral-800/60 transition-colors">
                    <td class="px-6 py-4 whitespace-nowrap text-white"><%= ticket.user_id ? ticket.user_id.email : 'N/A' %></td>
                    <td class="px-6 py-4 whitespace-nowrap font-mono text-sm text-gray-400"><%= ticket.ticket_code %></td>
                    <td class="px-6 py-4 whitespace-nowrap text-gray-300"><%= new Date(ticket.purchase_date).toLocaleString('th-TH') %></td>
                    <td class="px-6 py-4 whitespace-nowrap">
                       <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full 
                        <%= ticket.status === 'active' ? 'bg-green-600/30 text-green-300' : '' %>
                        <%= ticket.status === 'used' ? 'bg-yellow-600/30 text-yellow-300' : '' %>
                        <%= ticket.status === 'cancelled' ? 'bg-red-600/30 text-red-400' : '' %>
                      ">
                        <%= ticket.status %>
                      </span>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="4" class="text-center px-6 py-16 text-gray-500">
                    ยังไม่มีผู้ซื้อบัตรสำหรับกิจกรรมนี้
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>